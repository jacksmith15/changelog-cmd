branches:
  only:
    - main
environment:
  PYTHONPATH: .
language: python
python:
  - "3.9"
# command to install dependencies
install:
  - poetry install
# command to run tests
script:
  - poetry run inv verify
